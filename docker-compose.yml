version: "3.9"

services:
  teachify-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teachify-web
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
